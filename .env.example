# Enable development behavior for commands that import the app directly
FASTAPI_ENV=development

PROJECT_NAME="AI Nutrition Assistant"

SECRET_KEY=replace-with-a-secure-random-value
FIRST_SUPERUSER=admin@example.com
FIRST_SUPERUSER_PASSWORD=replace-with-a-secure-password

# Emails
SMTP_HOST=localhost
EMAILS_FROM_EMAIL=info@example.com
SMTP_TLS=False
SMTP_PORT=1025

# Postgres
POSTGRES_PASSWORD=replace-with-a-secure-password
DATABASE_URL=postgresql://postgres:${POSTGRES_PASSWORD}@localhost:5432/app
